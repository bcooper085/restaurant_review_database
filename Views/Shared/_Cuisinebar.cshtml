@if(Model["Cuisines"].Count == 0)
{
    <h4>There are no cuisines listed</h4>
}
else
{
    <h4><u>Cuisines</u></h4>
    foreach(var cuisine in Model["Cuisines"])
    {
        <h4><a href="/cuisines/@cuisine.GetId()">@cuisine.GetName()</a></h4>
    }
}
